# Newelle 1.4.5 configuration for Shesh (native install, not Flatpak).
# Native install is required for STDIO MCP + microphone + filesystem access.
# See docs/SHESH/06_SHESH_AGENT.md

[model]
provider = "ollama"
model = "phi4-mini"
ollama_url = "http://localhost:11434"

[voice]
enabled = true
wake_word = "hey shesh"
stt_backend = "faster_whisper"
stt_model = "base.en"          # ~145MB; English, fast. Use "small.en" if CPU allows.
tts_backend = "piper"
tts_voice = "en_US-ryan-high"

[memory]
enabled = true
embedding_model = "nomic-embed-text"

[permissions]
# Newelle 1.3.5+ file permissions
allow_read = [
  "~/Documents/Personal",
  "~/Downloads",
  "~/Notes",
  "~/Projects/personal",
]
allow_write = [
  "~/Downloads",
  "~/Documents/Inbox",
  "~/.local/share/shesh",
]
ask_before = ["~/"]
deny = [
  "~/Documents/Job",
  "~/Projects/job",
  "~/Vaults",
  "~/.ssh",
  "~/.gnupg",
]

# MCP servers — STDIO transport (Newelle 1.4.5 supports STDIO on native installs).
# These are the shesh-core console scripts installed by
# tools/install-shesh-stack.sh into ~/.local/bin/shesh-*-mcp (ADR-0019).
[mcp.system_control]
command = "~/.local/bin/shesh-system-mcp"
args = []
