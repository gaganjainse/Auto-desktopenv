# /etc/systemd/zram-generator.conf
# 16GB DDR5 -> 8GB compressed zram with zstd. See docs/SHESH/04_DEVICE_PROFILE.md
# If RAM is upgraded to 32/64GB, change zram-size to min(ram/4, 8192).
[zram0]
zram-size = ram/2
compression-algorithm = zstd
swap-priority = 100
